<div class="col-md-12">
  <h1 class="page-header text-center">Inscription</h1>
  <form action="{{ path('freelancer_registration') }}" {{ form_enctype(form) }} method="POST">
    {{ form_widget(form) }}
    <div>
      <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
  </form>
</div>