{% extends "WSUserBundle:Profile:show.html.twig" %}

{% block breadcrumb %}
    <li class="active">Projets sollicités</li>
{% endblock breadcrumb %}

{% block sideright %}

    {% if user.projectSolicites | length != 0  %}

        <div class="panel panel-default">
            <table id="_table_tool" class="table table-striped table-hover table-dynamic" cellspacing="0">
                <thead>
                <tr>
                    <th style="text-align: center;">Projet</th>
                </tr>
                </thead>
                <tbody id="_tbody">
                {% for ms in user.projectSolicites %}

                    <tr>
                        <td style="text-align: center;">{{ ms.project.object }}</td>
                    </tr>

                {% endfor %}
                </tbody>
            </table>
        </div>

    {% else %}

        <center>
            <h1>
                <span class="label label-danger">Aucune projets sollicités !</span>
            </h1>
        </center>

    {% endif %}

{% endblock sideright %}